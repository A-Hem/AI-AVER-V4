# requirements.txt
fastapi
uvicorn[standard]
python-dotenv # For managing API keys (optional but recommended)
python-multipart # For file uploads in FastAPI
#exiftool-13.32 # Requires exiftool system-wide installation
Pillow # For creating dummy images in metadata_analyzer test block
# google-generativeai # Uncomment if you enable actual Gemini calls
# requests # For weather API integration (future)
# scikit-learn # For AI-powered metadata analysis (future)
# tensorflow # For deep learning PRNU (future)
# pytorch # For deep learning PRNU (future)
